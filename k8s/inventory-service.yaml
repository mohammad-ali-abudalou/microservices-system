apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: inventory-service  # This field was missing in your configuration
  template:
    metadata:
      labels:
        app: inventory-service  # This must match the selector
    spec:
      dnsPolicy: ClusterFirst
      # Init Containers - Execute in order before main container starts
      initContainers:
        # Wait for Config Server to be ready
        - name: wait-for-config-server
          image: busybox:1.28
          command:
            - 'sh'
            - '-c'
            - |
              echo "Waiting for Config Server at config-server:8888..."
              until nc -z config-server 8888; do
                echo "Config Server not ready, waiting..."
                sleep 2
              done
              echo "Config Server is ready!"

        # Wait for Keycloak to be ready
        - name: wait-for-keycloak
          image: busybox:1.28
          command:
            - sh
            - -c
            - |
              echo "Waiting for Keycloak at keycloak-service:8080..."
              until nc -z keycloak-service 8080; do
                echo "Keycloak not ready, waiting..."
                sleep 2
              done
              echo "Keycloak is ready!"

        # Wait for MySQL database
        - name: wait-for-mysql
          image: busybox:1.28
          command:
            - 'sh'
            - '-c'
            - |
              echo "Waiting for MySQL at mysql-inventory:3306..."
              until nc -z mysql-inventory 3306; do
                echo "MySQL not ready, waiting..."
                sleep 2
              done
              echo "MySQL is ready!"

      containers:
        - name: inventory-service
          image: inventory-service:v1
          imagePullPolicy: IfNotPresent  # This line tells it: if the image exists locally, don't go to the internet
          ports:
            - containerPort: 8081
          envFrom:
            - configMapRef:
                name: microservices-config
          env:
            - name: KEYCLOAK_URL
              value: "http://keycloak-service:8080"
            - name: SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_ISSUER_URI
              value: "http://127.0.0.1:8080/realms/master"
            - name: SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_JWK_SET_URI
              value: "http://keycloak-service:8080/realms/master/protocol/openid-connect/certs"
            - name: SECURITY_OAUTH2_ACCEPTED_ISSUERS
              value: "http://127.0.0.1:8080/realms/master,http://keycloak-service:8080/realms/master"

            - name: SPRING_PROFILES_ACTIVE
              value: "k8s"
            # The correct connection URL for internal networking (always use 3306 here)
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:mysql://mysql-inventory:3306/inventory_service?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC"
            - name: SPRING_DATASOURCE_USERNAME
              value: "root"
            - name: SPRING_DATASOURCE_PASSWORD
              value: "root"
            # Force Hibernate to recognize the database type to bypass Metadata error
            - name: SPRING_JPA_DATABASE_PLATFORM
              value: "org.hibernate.dialect.MySQLDialect"
            - name: SPRING_JPA_HIBERNATE_DDL_AUTO
              value: "update"
            - name: SPRING_CLOUD_CONFIG_URI
              value: "http://config-server:8888"
            - name: SPRING_CONFIG_IMPORT
              value: "configserver:http://config-server:8888"
            - name: EUREKA_SERVER_URL
              value: "http://discovery-server:8761/eureka/"
            - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
              value: "http://discovery-server:8761/eureka/"
            - name: EUREKA_INSTANCE_HOSTNAME
              value: "inventory-service"
            - name: EUREKA_INSTANCE_PREFER_IP_ADDRESS
              value: "true"
            - name: EUREKA_INSTANCE_INSTANCE_ID
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: MANAGEMENT_TRACING_ENABLED
              value: "false"
            - name: MANAGEMENT_TRACING_SAMPLING_PROBABILITY
              value: "0.0"

---
apiVersion: v1
kind: Service
metadata:
  name: inventory-service
spec:
  selector:
    app: inventory-service
  ports:
    - port: 8081
      targetPort: 8081
